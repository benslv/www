---
import { getEntry, render } from "astro:content";
import Layout from "../layouts/Layout.astro";

const entry = await getEntry("pages", "index")!;

const { Content } = await render(entry);
---

<Layout image={entry.data.image}>
	<Content />
</Layout>
